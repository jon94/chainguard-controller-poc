apiVersion: security.chainguard.dev/v1
kind: ImagePolicy
metadata:
  name: jonlimpw-demo-policy
  namespace: default
spec:
  repository: "jonlimpw/cg-demo"
  checkIntervalSeconds: 10  # Fast check interval for responsive demo
  enforceLatestDigest: true
  namespaceSelector:
    matchLabels:
      monitored: "true"
  deploymentSelector:
    matchLabels:
      app: "cg-demo"
